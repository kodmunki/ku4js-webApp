/* Controller unit test template
 */

$(function() {

    module("controller.NAME");

    test("UNIT TEST NAME", function() {
        expect(1);
        var bundle = $.ku4webAppUT.bundle().throwErrors(),
            controller = bundle.controller("NAME");

        bundle.onModelCall("MODEL_METHOD_NAME", function (data) {
            equal(data, null);
        });
        controller.METHOD();
    });
});